{
  "name": "LoveCurrency",
  "version": "0.0.1",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . LoveCurrency --platform=darwin --arch=x64 --version=1.3.8 --overwrite"
  },
  "dependencies": {
    "request": "^2.76.0"
  }
}
